# Vue.js Project (Node.js)

Este repositorio contiene una aplicación frontend desarrollada con **Vue.js** y un servidor de desarrollo basado en **Vite**. A continuación se detallan los pasos necesarios para clonar, instalar dependencias y ejecutar el proyecto en un entorno local.

---

## 🚀 Requisitos Previos
Antes de comenzar, asegúrate de tener instalado:

- **Node.js** (versión LTS recomendada)
- **npm** (incluido con Node.js)
- **Git**

Puedes verificar las versiones instaladas con:
```bash
node -v
npm -v
```

---

## 📦 Instalación y Ejecución

### 1️⃣ Clonar el repositorio
```bash
git clone https://github.com/mauritalkie/Personal_Finance_App_Frontend.git
cd Personal_Finance_App_Frontend
```

---

### 2️⃣ Instalar dependencias
Dentro del proyecto clonado, ejecuta:
```bash
npm install
```
Esto instalará todos los paquetes necesarios definidos en `package.json`.

---

### 3️⃣ Ejecutar el servidor de desarrollo
```bash
npm run dev
```

---

## 🌐 Acceso a la aplicación
Una vez levantado el servidor, la aplicación estará disponible en:

- http://localhost:5173/

El servidor se recargará automáticamente al detectar cambios en el código.

